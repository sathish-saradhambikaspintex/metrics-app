import styled from "styled-components";
import * as VARS from "../_variables";

export default styled.div`
	flex: 1 1 auto;
	margin: ${VARS.gridSize * 2}px;
	line-height: ${VARS.gridSize * 1.8}px;
`;
